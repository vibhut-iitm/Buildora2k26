# Repository Agent Rules

## Git Workflow
- Always automatically stage (`git add .`), commit with a clear, descriptive message, and push (`git push origin main`) to GitHub after completing code edits or feature implementations without requiring explicit user instructions to push.
